[Definition]
# Ban IPs that trigger nginx 444 (scanner paths) or 429 (rate limit exceeded)
failregex = ^<HOST> .* "(GET|POST|HEAD) .* HTTP.*" (444|429) .*$
            ^<HOST> .* ".*\\x.* HTTP.*" (400|444) .*$

ignoreregex =
