# Origin lock (Scaleway stack): only Cloudflare's edge may connect. Direct hits on
# the instance IP would otherwise bypass Cloudflare's WAF/DDoS protection.
# Included inside the server blocks; $from_cloudflare comes from cloudflare-ips.conf.
if ($from_cloudflare = 0) {
    return 444;
}
