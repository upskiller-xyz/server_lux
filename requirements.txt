# Production dependencies for Model Server
# Generated from pyproject.toml


# Web Framework & Server
flask>=3.0.0,<4.0.0
# flask-cors >=6.0.0 fixes CVE-2024-6839 / CVE-2024-6844 / CVE-2024-6866
flask-cors>=6.0.0,<7.0.0
werkzeug>=3.0.0,<4.0.0


# HTTP Client
requests>=2.32.0,<3.0.0
aiohttp>=3.9.0,<4.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0,<4.0.0

# Utilities & Logging
# setuptools: >=78.1.1 fixes CVE-2024-6345 / CVE-2025-47273; >=81.0.0 is the
# first release whose vendored build deps are patched too — it bundles
# jaraco.context 6.1 (CVE-2026-23949) and wheel 0.46.3 (CVE-2026-24049) under
# setuptools/_vendor/, which scanners flag on older setuptools. Not imported at
# runtime, but kept installed so the image ships no vulnerable (vendored) copy.
setuptools>=81.0.0,<84.0.0
python-dotenv>=1.0.0,<2.0.0

# Computer Vision & Scientific Computing
opencv-python>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0

# Production Server (optional for Docker deployment)
gunicorn>=21.0.0,<23.0.0
flasgger

# Fast JSON parsing (request body / mesh can be several MB)
orjson>=3.9.0,<4.0.0